/* Same card as opengraph-image. The twitter:image URL hash comes from this file's
   contents, not the re-exported module, so touch this file when the card's look
   changes, otherwise X keeps serving its cached copy. Last redesign: light theme. */
export { default, alt, size, contentType } from './opengraph-image';
